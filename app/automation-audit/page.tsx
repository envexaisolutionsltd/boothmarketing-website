'use client'
import { FormEvent, useState } from 'react'
import { ArrowRight } from 'lucide-react'
import SiteHeader from '../../components/SiteHeader'
import SiteFooter from '../../components/SiteFooter'

export default function AutomationAuditPage(){const[status,setStatus]=useState<'idle'|'sending'|'success'|'error'>('idle');const[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('sending');setError('');const f=new FormData(e.currentTarget);const res=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({enquiryType:'AUTOMATION_AUDIT',name:f.get('name'),email:f.get('email'),company:f.get('company'),websiteUrl:f.get('websiteUrl'),challenge:f.get('challenge'),websiteCompany:f.get('websiteCompany')})});const data=await res.json().catch(()=>({}));if(res.ok){setStatus('success');e.currentTarget.reset()}else{setStatus('error');setError(data.error||'Unable to submit your request.')}}return <main className="min-h-screen bg-[#090a0b] text-[#f4f4f3]"><SiteHeader/><section className="py-14 sm:py-20"><div className="mx-auto grid w-[min(960px,calc(100%-28px))] gap-10 lg:grid-cols-[.85fr_1.15fr]"><div><p className="text-[10px] font-extrabold uppercase tracking-[0.24em] text-[#d8cbb7]">Automation Audit</p><h1 className="mt-4 text-[clamp(40px,8vw,58px)] font-semibold leading-[1.02] tracking-[-0.05em]">Start with the workflow, not the software.</h1><p className="mt-5 text-[14px] leading-7 text-[#929399]">Tell us where repetitive administration, handoffs, follow-up or data movement creates friction. The audit is designed to identify whether the process should be simplified, improved, automated or left alone.</p></div><form onSubmit={submit} className="rounded-[18px] border border-white/[0.08] bg-[#0d0f10] p-5 sm:p-7"><input name="websiteCompany" className="hidden" tabIndex={-1} autoComplete="off"/><div className="grid gap-4 sm:grid-cols-2">{[['name','Name *','text'],['email','Business email *','email'],['company','Company *','text'],['websiteUrl','Website','text']].map(([n,l,t])=><label key={n} className="text-[12px] text-[#a2a3a8]">{l}<input required={n==='name'||n==='email'||n==='company'} name={n} type={t} className="mt-2 min-h-[48px] w-full rounded-lg border border-white/[0.1] bg-[#090a0b] px-3 text-white outline-none focus:border-[#d8cbb7]"/></label>)}</div><label className="mt-4 block text-[12px] text-[#a2a3a8]">Where is the operational friction? *<textarea required name="challenge" rows={6} className="mt-2 w-full rounded-lg border border-white/[0.1] bg-[#090a0b] p-3 text-white outline-none focus:border-[#d8cbb7]" placeholder="Describe the repetitive work, handoffs or process you want reviewed."/></label>{status==='success'&&<p className="mt-4 text-[13px] text-[#d8cbb7]">Thanks — your Automation Audit request has been received.</p>}{status==='error'&&<p className="mt-4 text-[13px] text-red-300">{error}</p>}<button disabled={status==='sending'} className="mt-5 inline-flex min-h-[50px] items-center rounded-lg bg-[#efe3cf] px-5 text-[13px] font-semibold text-[#151515] disabled:opacity-60">{status==='sending'?'Submitting…':'Request Automation Audit'}<ArrowRight className="ml-2 h-4 w-4"/></button><p className="mt-4 text-[11px] leading-5 text-[#6f7075]">Your details are submitted securely to Booth Marketing so we can assess and respond to your request.</p></form></div></section><SiteFooter/></main>}
